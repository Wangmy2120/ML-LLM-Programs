# FatRat｜大模型算法岗深挖面试文档（面试官版）

> 目标：基于你在大模型/Agent/RAG/算法手撕方向的简历内容，构建一份“高覆盖、可追问、可实战演练”的面试题库。
> 使用方式：先按模块自测，再进行 30/45/60 分钟模拟。

---

## 0. 面试官评估维度（你需要对齐）

1. **项目真实性**：你是否真的主导过关键模块，还是只做过边缘工作。
2. **算法深度**：是否能把 Transformer、LoRA、PPO/DPO 从原理讲到实现细节。
3. **工程落地**：是否具备线上系统设计、稳定性治理、性能优化能力。
4. **结果导向**：是否能说出量化指标与业务价值，而不只讲“做了很多”。
5. **复盘能力**：是否能讲清失败案例、取舍逻辑与下一步优化。

---

## 1. 简历项目深挖（重点）

### 1.1 你的项目总览与职责边界

1. 你简历里最核心的大模型项目是什么？你在其中承担了哪些不可替代的职责？
2. 这个项目的业务目标是什么？为什么必须用 LLM，而不是传统检索/规则系统？
3. 如果只允许你保留 1 个技术亮点放在简历里，你会选哪个？为什么？

**追问链（真实性校验）**
- 你具体提交过哪些模块代码？
- 哪个关键决策是你拍板的？替代方案是什么？
- 如果把你从项目中移除，项目最可能在哪个环节出问题？

### 1.2 Agent 架构与工具调用

1. 你是如何定义 Agent 的系统边界（模型层、编排层、工具层、业务层）的？
2. Function Calling 如何做参数校验和权限控制？
3. 工具调用失败时，如何避免用户体验断崖式下降？

**追问链（深入）**
- 你如何防止 prompt injection 诱导越权调用？
- 退款/下单类高风险操作如何保证幂等性？
- 若订单服务与物流服务返回冲突结果，Agent 如何仲裁？

### 1.3 RAG 方案深挖

1. 你知识库切分策略如何设计（chunk 大小、重叠、结构化字段）？
2. 召回链路是“向量召回+关键词召回”还是单一路径？为什么？
3. 你如何评估 RAG 质量：看 recall、MRR，还是最终问答正确率？

**追问链（故障定位）**
- 命中正确文档但答案仍错误，根因排查顺序是什么？
- Top-k 固定还是动态？动态策略如何避免引入额外延迟？
- 知识库时效性如何保证？如何处理脏数据和冲突文档？

### 1.4 上下文管理与转人工

1. 多轮对话状态如何存储？为什么选 Redis（或其他）？
2. 你如何设计“低置信度转人工”阈值，避免过度转人工？
3. 转人工时如何传递上下文，减少用户重复描述成本？

**追问链（业务视角）**
- 智能率、转人工率、投诉率三者冲突时你优先级如何排？
- 如果用户情绪激烈但问题简单，是否仍应立即转人工？

---

## 2. 大模型算法八股 + 深挖追问

### 2.1 Transformer / Attention

1. 为什么 attention 要除以 `sqrt(d_k)`？
2. Pre-LN 和 Post-LN 的本质差异是什么？
3. KV Cache 的收益和代价分别是什么？
4. MHA、MQA、GQA 的计算/显存/效果如何权衡？

**高压追问**
- 为什么 KV Cache 缓存 K/V，不缓存 Q？
- RoPE 为什么更适合长上下文？
- causal mask 与 padding mask 叠加时张量维度如何对齐？

### 2.2 微调与参数高效训练（LoRA/QLoRA）

1. LoRA 为什么能用低秩近似完成任务迁移？
2. 你如何选择 rank、alpha、target modules？
3. 什么场景下 LoRA 不够，必须全参微调？

**高压追问**
- LoRA 合并与不合并部署的差异是什么？
- 多个 LoRA adapter 共存时如何处理冲突？

### 2.3 损失函数与优化

1. 熵、交叉熵、KL 散度关系？
2. AdamW 为什么要 decouple weight decay？
3. 梯度爆炸/消失如何排查与修复？

**高压追问**
- 混合精度下 loss scaling 的机制是什么？
- label smoothing 的收益与副作用是什么？

### 2.4 对齐训练（PPO / DPO）

1. PPO 的 clipped objective 解决了什么训练问题？
2. DPO 为什么不显式训练 reward model 也能优化偏好？
3. PPO 与 DPO 在工程复杂度和效果上如何取舍？

**高压追问**
- RLHF 里为什么通常要加 KL 约束？
- DPO 的 beta 参数变大/变小会带来什么行为变化？

---

## 3. 工程生产化高频问题（LLM 系统）

1. 你如何设计模型网关以支持多模型切换和故障转移？
2. 你如何做服务级降级（模型降级、上下文截断、工具熔断）？
3. 你如何做可观测性：日志、指标、链路追踪分别抓什么？
4. 你如何做提示词版本管理和灰度发布？
5. 你如何做成本优化（token、QPS、缓存命中率、GPU 利用率）？

**追问链（线上事故）**
- 讲一次你经历的线上故障：现象 -> 定位 -> 修复 -> 预防。
- 如果 SLA 要求 P95 < 1.5s，你会优先优化哪 3 个点？

---

## 4. 手撕代码可能考点（口述 + 代码）

1. 手写 `softmax`（数值稳定版本）。
2. 手写 `scaled dot-product attention`（含 mask）。
3. 手写 `LayerNorm` 与 `RMSNorm`。
4. 手写 `LoRA` 注入线性层的前向逻辑。
5. 手写 `KMeans`（初始化、迭代、停止条件）。
6. 手写 `PPO` 核心损失或 `DPO` 核心损失（伪代码）。

**追问链（必须会说）**
- 时间复杂度/空间复杂度是多少？
- 数值稳定性在哪里容易出错？
- batch 维与序列维广播如何处理？

---

## 5. 行为面 + 压力面（简历真实性验证）

1. 你最失败的一次实验是什么？当时你如何止损？
2. 你如何与产品、后端、算法同学对齐优先级？
3. 你的技术判断曾经被证明是错的吗？为什么？
4. 你是否有过“线上紧急回滚”的经历？流程如何？

**压力追问**
- 你说自己主导，那你的替代者需要多久才能接手？
- 给一个你做错决策的例子，不要只讲成功案例。

---

## 6. 面试官可能连续追问脚本（可自测）

### 30 分钟版

- 5 分钟：项目介绍（目标、职责、结果）。
- 10 分钟：项目深挖（架构、RAG、工具调用、转人工）。
- 10 分钟：算法深挖（Transformer + LoRA + PPO/DPO）。
- 5 分钟：开放题（故障、成本、安全、复盘）。

### 45 分钟版

- 在 30 分钟版基础上，增加：
- 10 分钟：手撕伪代码 + 复杂度解释。
- 5 分钟：行为面深挖（冲突、失败、领导力）。

### 60 分钟版

- 在 45 分钟版基础上，增加：
- 10 分钟：系统设计题（高并发低延迟客服 Agent）。
- 5 分钟：反问环节（你对团队/业务/技术栈的提问）。

---

## 7. 高频题“高分回答模板”（建议背诵）

> 每题按这 5 句结构回答，稳定且高效：

1. **结论**：先给明确观点。
2. **原理**：给公式/机制直觉。
3. **实战**：说你在项目里怎么做。
4. **结果**：给指标变化（如准确率、延迟、成本）。
5. **边界**：说明当前方案不足和下一步改进。

---

## 8. 快速查漏清单（面试前 24 小时）

1. Transformer：缩放、mask、KV Cache、GQA。
2. 微调：LoRA/QLoRA 适用边界。
3. 对齐：PPO vs DPO 的差异与应用条件。
4. RAG：切分、召回、重排、评估、幻觉治理。
5. 工程：熔断降级、幂等、安全、可观测性。
6. 表达：每题都说“结论 -> 证据 -> 复盘”。

---

## 9. 你现在可以直接开始的实战训练

1. 用 3 分钟讲完你的核心项目（录音）。
2. 从第 1 节随机抽 5 题，每题回答 60 秒。
3. 从第 2 节随机抽 5 题，每题写 5 行要点。
4. 从第 4 节选 2 题，现场手写伪代码并口述复杂度。

> 建议：把所有答案都补上“量化数字”（例如：准确率 +X%、P95 降低 Y ms、转人工率下降 Z%）。没有数字，面试官通常默认深度不足。

---

## 10. 结合问题的详细回答（可直接背诵）

> 使用方法：优先掌握 P0（高频高压）问题；每题按“结论 -> 原理 -> 实战 -> 指标 -> 边界”输出。

### 10.1 P0 项目深挖题（建议 60s/题）

**Q1：为什么这个项目必须用 LLM，而不是规则系统？**
**答：**
- 结论：电商客服问题长尾明显、表达多样，纯规则覆盖成本高，LLM 具备更强泛化能力。
- 原理：规则系统擅长确定性流程，LLM 擅长语义理解与意图归纳；二者结合才能兼顾稳定与智能。
- 实战：我采用“意图识别 + RAG + 工具调用”三段式，咨询类走知识检索，交易类走 API 执行。
- 指标：通常重点看首问解决率（FCR）、转人工率、工具成功率、P95 延迟。
- 边界：高风险动作不能仅靠模型，需要后端权限校验和人工兜底。

**Q2：你在项目中的不可替代贡献是什么？**
**答：**
- 结论：我主导了核心链路的架构落地与质量闭环，而非只做单点功能。
- 原理：LLM 项目成败不只在模型效果，还取决于检索质量、工具稳定性、线上可观测性。
- 实战：我负责从会话入口到回答输出的全链路设计，重点解决“误召回、工具失败、过度转人工”。
- 指标：可用智能率、转人工率下降、P95 稳定性提升来证明贡献。
- 边界：如果缺少业务数据治理，模型上限会受限，需要持续建设标注和评测集。

**Q3：Function Calling 如何做安全控制？**
**答：**
- 结论：安全必须“模型外”保证，不能信任模型自觉。
- 原理：模型可被提示注入影响，因此执行层必须做白名单、参数 Schema、权限二次校验。
- 实战：我会把工具声明严格结构化，执行前做用户身份和资源归属校验，执行后记录审计日志。
- 指标：关注越权拦截率、误拦截率、高风险调用告警数。
- 边界：仍需定期红队测试，不同提示注入手法要持续更新防护策略。

**Q4：RAG 命中文档但答案错，怎么排查？**
**答：**
- 结论：按“召回 -> 重排 -> 生成引用”分层定位，避免盲目调参。
- 原理：答案错误不等于召回错误，可能是重排顺序不佳或生成阶段未正确引用证据。
- 实战：先检查 top-k 文档相关性，再看模型是否引用 top 证据；必要时加引用约束与 rerank。
- 指标：看 groundedness（答案-证据一致性）、人工抽检准确率、幻觉率。
- 边界：时效性强场景必须引入文档新鲜度权重，否则旧知识会污染输出。

**Q5：如何降低过度转人工？**
**答：**
- 结论：不能单阈值硬切，需要“澄清提问 + 多特征打分”。
- 原理：很多低置信度来自用户信息不完整，而不是系统无能力回答。
- 实战：我会先自动追问补全关键信息，再结合召回分、工具可执行性、历史轮次一致性综合判定。
- 指标：目标是提升智能解决率，同时保持投诉率不升高。
- 边界：退款、投诉等高风险意图仍应保守，优先安全与可追溯。

### 10.2 P0 算法八股题（建议 45~60s/题）

**Q6：Attention 为什么除以 sqrt(d_k)？**
**答：**
- 结论：防止点积随维度增大导致 softmax 饱和、梯度变小。
- 原理：q·k 的方差与 d_k 相关，缩放后 logits 分布更稳定，有利于训练。
- 实战：实现时我固定 scale，并配合减去 max 的 softmax 稳定写法。
- 指标：训练曲线抖动更小，收敛更平滑。
- 边界：超长序列场景还需配合高效注意力或分块策略。

**Q7：Pre-LN 与 Post-LN 差异？**
**答：**
- 结论：Pre-LN 训练更稳，Post-LN 在部分配置可能表达更强但更难训。
- 原理：Pre-LN 让残差路径更通畅，深层网络梯度传播更友好。
- 实战：工程上我优先 Pre-LN，特别是层数较深、数据噪声较大时。
- 指标：通常表现为训练更稳定、爆梯度概率更低。
- 边界：具体选型仍要结合任务和模型规模做对比实验。

**Q8：KV Cache 为什么只缓存 K/V？**
**答：**
- 结论：历史 K/V 会被反复复用，历史 Q 不会复用，缓存 Q 价值低。
- 原理：自回归解码每一步只新增一个 token 的 Q，但要与所有历史 K/V 计算注意力。
- 实战：我按 layer/head/batch 维护 K/V 缓存，并处理长度增长与显存管理。
- 指标：吞吐提升明显，单 token 解码延迟下降。
- 边界：长上下文下显存压力上升，需要分页或窗口化缓存。

**Q9：LoRA 为什么有效？**
**答：**
- 结论：任务迁移的有效更新通常位于低秩子空间，低秩分解可近似全参更新。
- 原理：冻结原权重，仅训练 A、B 两个低秩矩阵，显著减少可训练参数。
- 实战：优先在 attention 投影层加 LoRA，按任务复杂度调 rank 和 alpha。
- 指标：显存占用和训练成本下降，效果接近全参微调。
- 边界：跨领域跨度大时低秩容量不足，可能需提高 rank 或切全参。

**Q10：PPO 与 DPO 如何取舍？**
**答：**
- 结论：DPO 工程更简洁稳定，PPO 更灵活但复杂度更高。
- 原理：PPO 依赖在线采样与奖励模型；DPO 直接在偏好对上优化策略相对偏好。
- 实战：迭代周期紧、数据质量较好时优先 DPO；需细粒度行为控制时考虑 PPO。
- 指标：DPO 上线速度更快；PPO 在特定 reward 目标下上限更高。
- 边界：DPO 非常依赖偏好数据质量，脏数据会直接误导优化方向。

### 10.3 P0 工程生产化题（建议 60s/题）

**Q11：如何把 P95 延迟压到目标线内？**
**答：**
- 结论：先分解链路，再按“高收益低风险”优先级优化。
- 原理：延迟常由检索、模型推理、外部 API 三段组成，必须逐段定位。
- 实战：我会优先做缓存命中优化、上下文裁剪、模型路由分层；并设置超时熔断与降级。
- 指标：看 P50/P95/P99、错误率、转人工率联动变化。
- 边界：过度降级会伤害回答质量，要通过 A/B 保证体验不退化。

**Q12：如何设计可观测性？**
**答：**
- 结论：必须同时建设日志、指标、链路追踪三件套。
- 原理：日志用于定位个案，指标用于监控趋势，追踪用于定位瓶颈环节。
- 实战：我会为每次会话打 trace_id，串联用户输入、检索结果、模型输出、工具调用。
- 指标：重点监控请求量、延迟分位、工具失败率、幻觉率、转人工率。
- 边界：日志要做脱敏和分级留存，避免隐私泄露与存储失控。

**Q13：线上故障如何复盘？**
**答：**
- 结论：复盘核心是“可重复、可归因、可预防”。
- 原理：事故治理不是写总结，而是补齐监控、流程、预案和自动化校验。
- 实战：我按“现象 -> 时间线 -> 根因 -> 修复 -> 防再发”模板输出，并补压测和告警规则。
- 指标：看同类故障复发率、平均恢复时长（MTTR）、告警误报率。
- 边界：若没有演练机制，文档化复盘很难真正落地。

### 10.4 行为面高分答法（建议 45s/题）

**Q14：讲一个失败案例。**
**答：**
- 结论：我会选“有业务损失但可复现可改进”的失败，不回避责任。
- 原理：面试官看的是问题处理能力，不是零失误神话。
- 实战：说明当时误判原因、止损动作、后续机制改进（如评测集/灰度/回滚开关）。
- 指标：给出修复前后差异，如故障时长下降、误触发率下降。
- 边界：承认当时认知不足，并说明后来如何系统补课。

**Q15：如何证明你是主导者？**
**答：**
- 结论：用“职责边界 + 关键决策 + 量化结果”三证合一。
- 原理：头衔不重要，交付闭环才是主导证据。
- 实战：讲 1 个你独立推进的关键改造，含背景、阻力、推进方式、结果。
- 指标：至少给 2 个硬指标和 1 个跨团队影响。
- 边界：明确哪些成果是团队共创，避免过度归因。

---

## 11. 二面/三面高压追问矩阵（再深一层）

> 用法：先回答主问题，再主动补一句“风险与取舍”，可显著提升高级感。

### 11.1 项目真实性高压矩阵

**主问：你说主导了项目，主导体现在哪？**
- 连问 1：如果把你从项目里移除，哪 3 个模块会先出问题？
- 连问 2：你做过最关键的 trade-off 是什么？为什么不是另一个方案？
- 连问 3：有没有你拍板但后来证明不够好的决策？
- 收束答法：`职责边界 -> 决策依据 -> 量化结果 -> 失败复盘`。

**主问：你说做过 RAG 优化，具体优化了什么？**
- 连问 1：你改了哪些参数（chunk、top-k、rerank、prompt）？
- 连问 2：每项优化的收益和副作用是什么？
- 连问 3：如何证明提升不是偶然波动？
- 收束答法：`变量隔离实验 -> 指标变化 -> 回归验证`。

### 11.2 算法深度高压矩阵

**主问：你理解 DPO 吗？**
- 连问 1：请写 DPO 核心目标的直觉解释。
- 连问 2：beta 调大/调小分别会发生什么？
- 连问 3：为什么 DPO 可能比 PPO 更稳定但上限不一定更高？
- 收束答法：`目标函数直觉 -> 参数影响 -> 工程取舍`。

**主问：你理解 GQA 吗？**
- 连问 1：它比 MHA 省了什么？损失了什么？
- 连问 2：在长上下文推理时为什么更常见？
- 连问 3：如果效果下降，你优先补什么（头数、训练策略、数据）？
- 收束答法：`复杂度分析 -> 显存收益 -> 质量回补策略`。

### 11.3 工程落地高压矩阵

**主问：线上延迟飙升怎么处理？**
- 连问 1：你如何 10 分钟内止血？
- 连问 2：如何定位是检索慢、模型慢还是外部 API 慢？
- 连问 3：你会牺牲哪些能力换 SLA？哪些不能牺牲？
- 收束答法：`止血降级 -> 链路拆解 -> 根因修复 -> 防再发`。

**主问：你如何防提示注入？**
- 连问 1：仅靠 system prompt 为什么不够？
- 连问 2：工具调用层如何做强约束？
- 连问 3：如何持续验证防护有效？
- 收束答法：`模型外防护 + 权限校验 + 红队演练`。

---

## 12. 系统设计题标准答法（SLA / 成本 / 质量三角）

### 12.1 题目模板：设计高并发低延迟智能客服 Agent

**标准作答骨架（建议 3~5 分钟）**
1. 目标定义：QPS、P95、可用性、转人工率、准确率目标。
2. 架构拆分：接入层、编排层、检索层、工具层、会话层、监控层。
3. 关键路径：`输入 -> 意图识别 -> 路由 -> RAG/工具 -> 安全校验 -> 输出`。
4. 稳定性：限流、超时、重试、熔断、降级、多模型回退。
5. 数据闭环：离线评测、在线 A/B、问题回流标注。
6. 安全合规：PII 脱敏、最小权限、审计日志、敏感操作二次确认。

### 12.2 高分加分点（面试官最看重）

- 能清晰说出“哪些请求允许降级，哪些必须失败快返”。
- 能定义“分级 SLA”：普通咨询、订单查询、退款操作分别设不同目标。
- 能量化成本：token 成本、缓存命中率、向量检索成本、模型路由成本。
- 能给出容量规划：峰值流量下的扩容策略与压测口径。

### 12.3 常见追问与参考回答

**追问：为什么你不把所有请求都走大模型？**
- 参考答：高频确定性问题应优先规则/模板或小模型，减少成本和时延；复杂问题再升级到大模型，保证性价比。

**追问：如何保证退款类操作安全？**
- 参考答：模型只负责理解与组装意图，真正执行必须经过后端鉴权、参数校验、幂等键、审计日志和人工兜底。

---

## 13. 数据与评测闭环（很多候选人会漏）

### 13.1 离线评测集建设

- 样本结构：意图分类样本、知识问答样本、工具调用样本、对抗注入样本。
- 标注维度：正确性、完整性、引用一致性、安全性、可执行性。
- 切分原则：按业务场景与时间切片，避免信息泄漏。

### 13.2 在线评测与 A/B

- 线上核心指标：FCR、转人工率、P95、投诉率、工具成功率。
- 试验设计：明确实验窗口、样本量、显著性阈值、回滚条件。
- 风险控制：高风险意图默认灰度，配置紧急开关与流量闸门。

### 13.3 回归门禁（上线前必须有）

- 门禁 1：核心问答准确率不低于基线。
- 门禁 2：高风险工具调用误触发率低于阈值。
- 门禁 3：延迟与成本不超过预算。
- 门禁 4：提示注入测试集通过率达标。

### 13.4 你可以直接背的闭环一句话

`我们用离线集做方向判断，用在线 A/B 做真实收益确认，用回归门禁防止版本倒退，形成“评测-上线-回流-再训练”闭环。`

---

## 14. 更细的答案增强卡（把“会答”变“答得像做过”）

### 14.1 每个回答都补 1 句“反例”

- 示例：`Top-k 不是越大越好，过大可能引入噪声，反而降低答案稳定性。`

### 14.2 每个回答都补 1 句“失败经历”

- 示例：`我们曾因只看离线准确率上线，导致线上投诉上升，后续加入了投诉率门禁。`

### 14.3 每个回答都补 1 句“取舍原则”

- 示例：`在退款场景我优先安全与可追溯，即使牺牲部分自动化率。`

### 14.4 每个回答都补 1 个“数字证据”

- 示例：`通过缓存和路由分层，P95 从 2.1s 降到 1.4s。`

---

## 15. 反问面试官模板（收尾拉分）

1. 你们当前线上最核心的 LLM 指标是质量优先还是成本优先？
2. 团队在 RAG 侧更关注召回优化还是知识治理？
3. 你们对 DPO/PPO 的使用边界是如何定义的？
4. 目前系统最痛的瓶颈是延迟、幻觉还是工程稳定性？
5. 入职后 3 个月，这个岗位最期望我交付什么结果？

> 高分技巧：反问要围绕“业务目标 + 技术挑战 + 交付预期”，避免泛泛而谈。
